# Contributing

This is a fork of Common Voice that tracks the upstream production branch.

## Updating upstream

```bash
git remote add upstream git@github.com:common-voice/common-voice.git

git pull --rebase upstream production
```
